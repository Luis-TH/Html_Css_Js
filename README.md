<!-- 
22/10/16
Titulos <h*>
ul - lista ordenada, osea con numero
ol - lista desordenada, sin numero, pero si con un elemento a la izquierda
li - elementos de una lista
imagenes con elementos : src, alt
tablas, si le paso el atributo border con un numero que es el tamaño de la linea
<tr>: Es una fila de la tabla
<td>: Elementos de la tabla, se muestran continuos, osea como columnas, colspan aumenta el tamaño
<th>: Es para poner en negritas el elemento que tenga dentro

Formulario
<form>: Crea un formulario, action: que accion hace el formulario, method: tipo de metodo http
<label>: Es una etiqueta, donde puedo introducir mostrar un texto. 
        for: Nececita name de otro campo para saber a donde apunta
<input>: donde puede escribir y su type es que tipo de contenido tiene. name: para apuntar con el label
        placeholder: Escribir algo dentro del campo en gris
<textarea>: Caja de texto
<input>:
        type: De que tipo es, radio es para seleccionar
        name: Nombre del campo
        value: valor inicial
<select>: Para seleccionar, osea se abren opciones
    option: Opciones dentro del select
            value: valor del campo
<input>
        type: submit es un boton
        value: Lo que dice dentro
<a href>: Nos redirige a una pagina
        target: para decir si quiero que sea una pestaña nueva, no en la misma
Si ha una etiqueta le asigno el atributo style le puedo poner lo que yo quiera de CSS
 -->